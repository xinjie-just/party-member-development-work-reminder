<view class="page">
    <view class="header">
        <text class="welcome">您好，XXXXX</text>
        <text wx:if="{{!noTodoList}}" class="tips">您有如下代办事项</text>
        <text wx:if="{{noTodoList}}" class="tips">您暂无代办事项</text>
    </view>
    <view class="todo-container">
        <block wx:if="{{!noTodoList}}">
            <view class="todo-list">
                <text class="todo-item" wx:for="{{todoList}}" wx:key="index" catchtap="selectTodoItem">{{item}}</text>
            </view>
            <button type="button" class="submit-btn">去处理</button>
        </block>
        <image src="../../assets/images/img_nodata.png" class="no-data-img" wx:if="{{noTodoList}}"></image>
    </view>
</view>